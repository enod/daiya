<%= simple_form_for(@negotiation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content, required: true, label:false %>
    <%= f.hidden_field :sender_id, required: true, value: @sender.id %>

  </div>

  <div class="form-actions">
    <%= f.button :submit, "Үүсгэх" %>
  </div>
<% end %>

