<div class="row">
  <div class="col-md-3">
    <p><strong>Юу: </strong>
    <%= @receiver.stuff %></p>
    <p>
      <strong>Description:</strong>
      <%= @receiver.description %>
    </p>
    <p>
      <strong>Source address:</strong>
      <%= @receiver.source_address %>
    </p>

    <p>
      <strong>Destination address:</strong>
      <%= @receiver.destination_address %>
    </p>
    <div id="map-canvas2" style="width: 230px; height:230px;" > </div>
    <p>
      <strong>From:</strong>
      <%= @receiver.from %>
    </p>

    <p>
      <strong>To:</strong>
      <%= @receiver.to %>
    </p>

    <p>
      <strong>Phone:</strong>
      <%= @receiver.phone %>
    </p>

    <p>
      <strong>Mail:</strong>
      <%= @receiver.mail %>
    </p>

    <p>
      <strong>Fee:</strong>
      <%= @receiver.fee %>
    </p>

<i class="fa fa-trash-o"> <%= link_to 'устгах', receiver_path(@receiver), :confirm => 'Are you sure?', :method => :delete %> </a></i> |
<i class="fa fa-pencil"> <%= link_to 'засах', edit_receiver_path(@receiver) %></a></i> |
<i class="fa fa-arrow-left"> <%= link_to 'буцах', receivers_path %></a></i>  


  </div>

  <div class="col-md-9">

    <h3>Хүргэх саналууд</h3>
    <% if current_user %>
    <h5>Шинэ санал</h5>
    <%= render "requests/form" %>
    <h5>Бүх саналууд</h5>
    <%= render @requests %>
    <% end %>
  </div>

</div>


<div id="map-canvas1" style="width: 230px; height:230px;" > </div>


 


<script type="text/javascript">
      function initialize() {
        
        var dest = {lat: <%= @receiver.latitude2 %>, lng: <%= @receiver.longitude2 %>};
        
        
        var mapOptions2 = {
          center: dest,
          zoom: 8
        };
        
        
        var map2 = new google.maps.Map(document.getElementById('map-canvas2'), mapOptions2);
        
        
        var marker2 = new google.maps.Marker({
          position:dest,
          map: map2
        });
      }
      google.maps.event.addDomListener(window, 'load', initialize);
</script>
