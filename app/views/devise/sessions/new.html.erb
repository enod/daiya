<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-signin"}) do |f| %>
  <h2 class="text-center"> Нэвтрэх </h2>
    <%= f.input :email, required: false, autofocus: true, label: false, placeholder: "Майл", input_hmtl: {class: "form-control"} %>
    <%= f.input :password, required: false, label: false, placeholder: "Нууц үг", input_hmtl: {class: "form-control"} %>
    <%= f.button :submit, "Нэвтрэх", class: "btn btn-primary btn-block" %>
  
<% end %>

<%= render "devise/shared/links" %>
